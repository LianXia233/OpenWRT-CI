#科学插件调整
CONFIG_PACKAGE_luci-app-homeproxy=y
CONFIG_PACKAGE_luci-app-nikki=y

#模组管理插件
# >> QModem 核心组件
CONFIG_PACKAGE_luci-app-qmodem=y                # 核心模组管理
CONFIG_PACKAGE_luci-app-qmodem-sms=y            # 短信功能
CONFIG_PACKAGE_luci-app-qmodem-ttl=y            # TTL重写

# << QModem

# >> 模组驱动配置
CONFIG_PACKAGE_kmod-mhi=y                       # PCIe模组支持
CONFIG_PACKAGE_qfirehose=y                      # 高通固件烧录
CONFIG_PACKAGE_quectel-cm-tom=y                 # 定制版连接管理器
CONFIG_PACKAGE_kmod-qmi_wwan_q=y                # 通用QMI驱动
# << 驱动

#增加插件
CONFIG_PACKAGE_luci-app-samba4=y
CONFIG_PACKAGE_luci-app-wolplus=y
CONFIG_PACKAGE_luci-app-gecoosac=y
CONFIG_PACKAGE_luci-app-tailscale=y             # 与Wireguard共存
CONFIG_PACKAGE_luci-app-autoreboot=y

#删除插件
CONFIG_PACKAGE_luci-app-wol=n
CONFIG_PACKAGE_luci-app-ddns=n
CONFIG_PACKAGE_luci-app-vlmcsd=n
CONFIG_PACKAGE_luci-app-xlnetacc=n
CONFIG_PACKAGE_luci-app-zerotier=n
# CONFIG_PACKAGE_luci-app-wireguard=n           # 需保留给Tailscale
CONFIG_PACKAGE_luci-app-ipsec-vpnd=n
CONFIG_PACKAGE_luci-app-adbyby-plus=n
CONFIG_PACKAGE_luci-app-filetransfer=n
CONFIG_PACKAGE_luci-app-unblockmusic=n
CONFIG_PACKAGE_luci-app-accesscontrol=n
CONFIG_PACKAGE_luci-app-fileassistant=n

#参数调整
CONFIG_DEVEL=y
CONFIG_CCACHE=y
CONFIG_TARGET_MULTI_PROFILE=y
CONFIG_TARGET_PER_DEVICE_ROOTFS=y

#内核调整（优化USB驱动层级）
CONFIG_PACKAGE_kmod-usb-net-=y                  # 清理重复配置
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=y          # 保留QMI核心驱动
CONFIG_PACKAGE_kmod-usb-net-huawei-cdc-ncm=y
CONFIG_PACKAGE_kmod-usb-net-rndis=y
CONFIG_PACKAGE_kmod-usb-net-rtl8152=y
CONFIG_PACKAGE_kmod-usb-storage-uas=y

#精简过时驱动
# CONFIG_PACKAGE_kmod-usb-ehci=n
# CONFIG_PACKAGE_kmod-usb-ohci=n
# CONFIG_PACKAGE_kmod-usb-uhci=n

#组件调整（新增必要工具）
CONFIG_PACKAGE_usbutils=y                       # USB设备检测工具
CONFIG_PACKAGE_comgt-ncm=y                      # 拨号脚本支持
CONFIG_PACKAGE_umbim=y                          # MBIM协议支持

#保留核心组件
CONFIG_PACKAGE_autocore=y
CONFIG_PACKAGE_automount=y
CONFIG_PACKAGE_bash=y
CONFIG_PACKAGE_ip-full=y
CONFIG_PACKAGE_luci-base=y
CONFIG_PACKAGE_luci-compat=y
